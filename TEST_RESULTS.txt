Hex Settlers validation results

Change: Add self-service user management for every account, verified recovery-email linking, and pre-login email password resets while preserving administrator-wide management for Benleethom.

Security behavior:
  - Non-administrator users receive only their own account record.
  - Self-service password changes require the current password.
  - Administrator resets and self-service password changes revoke older saved sessions.
  - Email verification links expire after 1 hour and are single use.
  - Password-reset links expire after 30 minutes and are single use.
  - Password hashes, session tokens, and one-time tokens are never returned to the browser.
  - Email and username reset requests use non-enumerating responses and rate limits.
  - Recovery email can use Resend or TLS-capable SMTP.

Validation command:
  npm run check

Result:
  132 tests passed
  0 tests failed
  TypeScript bootstrap compiled successfully
  server.js syntax check passed
  public/app.js syntax check passed
