{
  "name": "hex-settlers",
  "version": "1.0.0",
  "private": true,
  "description": "4-player Catan-style online web game (WebSocket). (Railway TS bootstrap hardening)",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "start:legacy": "node server.js",
    "build:bootstrap": "npx tsc -p tsconfig.bootstrap.json"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}